// Выполняем эту команду, делаем коммит и пушим на удаленный репозиторий и там удалется readme.txt
git rm --cached readme.txt

// А это нужно, чтобы удалить изменения файла из отслеживания до коммита (но верхняя команда используется отдельно от этой)
git restore --staged <файл>
